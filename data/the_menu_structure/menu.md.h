add_item(list, "Root item 1", NULL);
add_list(list, list->edge);
list = list->edge->child;
add_item(list, "Item 1.1 (leaf)", NULL);
add_item(list, "Item 1.2 (leaf)", NULL);
add_item(list, "Item 1.3 (leaf)", NULL);
add_item(list, "back", NULL);
list->edge->child = list->parent;
list = list->parent;
add_item(list, "Root item 2 (leaf)", NULL);
add_item(list, "Root item 3 (leaf)", NULL);
add_item(list, "Root item 4 (leaf)", NULL);
add_item(list, "Root item 5", NULL);
add_list(list, list->edge);
list = list->edge->child;
add_item(list, "Item 5.1 (leaf)", NULL);
add_item(list, "Item 5.2", NULL);
add_list(list, list->edge);
list = list->edge->child;
add_item(list, "Item 5.2.1 (leaf)", NULL);
add_item(list, "back", NULL);
list->edge->child = list->parent;
list = list->parent;
add_item(list, "Item 5.3", NULL);
add_list(list, list->edge);
list = list->edge->child;
add_item(list, "Item 5.3.1", NULL);
add_list(list, list->edge);
list = list->edge->child;
add_item(list, "Item 5.3.1.1 (leaf)", NULL);
add_item(list, "back", NULL);
list->edge->child = list->parent;
list = list->parent;
add_item(list, "back", NULL);
list->edge->child = list->parent;
list = list->parent;
add_item(list, "back", NULL);
list->edge->child = list->parent;
list = list->parent;
add_item(list, "Root item 6 (leaf)", NULL);
add_item(list, "Root item 7 (leaf)", NULL);
add_item(list, "Root item 8 (leaf)", NULL);
add_item(list, "Root item 9 (leaf)", NULL);
add_item(list, "Root item 10 (leaf)", NULL);
add_item(list, "Root item 11 (leaf)", NULL);
add_item(list, "Shut down.", exit);
